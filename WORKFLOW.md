# 招聘信息采集系统 - 业务流程文档

## 📋 目录

1. [项目背景](#项目背景)
2. [业务目标](#业务目标)
3. [技术架构](#技术架构)
4. [数据采集流程](#数据采集流程)
5. [数据处理流程](#数据处理流程)
6. [使用流程](#使用流程)
7. [后续优化方向](#后续优化方向)

---

## 项目背景

### 需求来源
- **痛点**: 各大平台（知乎、小红书、B站等）有大量公司员工、HR发布的招聘信息和内推码，但分散且难以系统化收集
- **传统方式**: 手动搜索、收藏、整理 - 耗时耗力且容易遗漏
- **目标**: 自动化采集多平台招聘信息，结构化存储，便于筛选和分析

### 为什么选择这些平台？

| 平台 | 招聘信息类型 | 优势 |
|------|------------|------|
| **知乎** | 公司官方回答、员工内推、技术讨论 | 信息质量高、权威性强 |
| **小红书** | HR招聘帖、内推码、公司文化宣传 | 图文并茂、真实性强 |
| **B站** | 公司官方宣传片、校招信息 | 视频形式、生动直观 |
| **微博** | 官方招聘公告、实时动态 | 传播快、更新及时 |

---

## 业务目标

### 核心目标
1. **自动化采集**: 定时/按需采集多平台招聘信息
2. **结构化存储**: 统一格式存储，便于查询和分析
3. **智能筛选**: 根据关键词、热度、公司等维度筛选
4. **长期监控**: 持续跟踪目标公司的招聘动态

### 业务价值

```
┌─────────────────────────────────────────────────┐
│             个人求职者视角                       │
├─────────────────────────────────────────────────┤
│ ✓ 获取一手内推信息                              │
│ ✓ 了解目标公司招聘动态                          │
│ ✓ 提高求职效率和成功率                          │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│             数据分析视角                         │
├─────────────────────────────────────────────────┤
│ ✓ 行业招聘趋势分析                              │
│ ✓ 薪资水平统计                                  │
│ ✓ 技能需求变化追踪                              │
└─────────────────────────────────────────────────┘
```

---

## 技术架构

### 系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                    用户层                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐            │
│  │ 命令行   │  │ 运行脚本 │  │ 配置文件 │            │
│  └─────┬────┘  └─────┬────┘  └─────┬────┘            │
└────────┼──────────────┼──────────────┼────────────────┘
         │              │              │
         ▼              ▼              ▼
┌─────────────────────────────────────────────────────────┐
│                    应用层                               │
│  ┌───────────────────────────────────────────┐         │
│  │         MediaCrawler 爬虫框架             │         │
│  │  ┌────────┐  ┌────────┐  ┌────────┐     │         │
│  │  │ 登录   │→ │ 签名   │→ │ 数据   │     │         │
│  │  │ 模块   │  │ 生成   │  │ 采集   │     │         │
│  │  └────────┘  └────────┘  └────────┘     │         │
│  └───────────────────────────────────────────┘         │
└─────────────────────────────────────────────────────────┘
                        │
         ┌──────────────┼──────────────┐
         ▼              ▼              ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│   Playwright │ │    HTTPX     │ │   数据存储   │
│  浏览器自动化│ │   HTTP客户端 │ │   JSON/CSV   │
└──────────────┘ └──────────────┘ └──────────────┘
                        │
         ┌──────────────┼──────────────┐
         ▼              ▼              ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│    知乎      │ │   小红书     │ │    B站等     │
│   API/页面   │ │  API/页面    │ │  API/页面    │
└──────────────┘ └──────────────┘ └──────────────┘
```

### 核心技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| **爬虫框架** | MediaCrawler | 多平台数据采集 |
| **浏览器自动化** | Playwright | 模拟真实用户行为 |
| **HTTP客户端** | HTTPX | API请求 |
| **数据存储** | JSON/CSV/DB | 多格式存储支持 |
| **语言** | Python 3.12+ | 开发语言 |

### 技术创新点

```
传统爬虫 vs MediaCrawler

┌─────────────────────────────────────────────────┐
│             传统爬虫                             │
├─────────────────────────────────────────────────┤
│ 1. 逆向JS加密算法 → 复杂                         │
│ 2. Python重写加密逻辑 → 维护成本高               │
│ 3. 平台更新即失效 → 不稳定                       │
│ 4. 需要专业逆向知识 → 门槛高                     │
└─────────────────────────────────────────────────┘

                    vs

┌─────────────────────────────────────────────────┐
│           MediaCrawler                          │
├─────────────────────────────────────────────────┤
│ 1. 利用浏览器环境 → 简单                         │
│ 2. 调用页面JS函数 → 零维护                      │
│ 3. 平台更新仍可用 → 稳定                        │
│ 4. 只需基础编程知识 → 门槛低                    │
└─────────────────────────────────────────────────┘
```

---

## 数据采集流程

### 完整流程图

```
┌──────────────────────────────────────────────────┐
│              1. 准备阶段                         │
├──────────────────────────────────────────────────┤
│  ▶ 读取配置文件 (base_config.py)                 │
│  ▶ 加载关键词列表                                │
│  ▶ 检查登录状态                                  │
└──────────────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────┐
│              2. 登录阶段                         │
├──────────────────────────────────────────────────┤
│  ┌────────────┐    ┌────────────┐              │
│  │  方式1:    │    │  方式2:    │              │
│  │  二维码    │    │  Cookie    │              │
│  │  扫码登录  │    │  直接登录  │              │
│  └────────────┘    └────────────┘              │
│           │                │                    │
│           ▼                ▼                    │
│     打开浏览器          读取Cookie              │
│     显示二维码          设置请求头               │
│     用户扫码            直接访问                 │
│           │                │                    │
│           └────────┬───────┘                    │
│                    ▼                            │
│          保存登录态到浏览器                     │
└──────────────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────┐
│              3. 签名生成阶段 (核心)              │
├──────────────────────────────────────────────────┤
│  传统方式 (复杂):                                 │
│    逆向JS → 分析加密 → Python实现 → 易失效       │
│                                                  │
│  MediaCrawler方式 (简单):                        │
│    page.evaluate("window.mnsv2(...)")           │
│    └─ 直接调用浏览器JS函数获取签名               │
│                                                  │
│  生成签名参数:                                   │
│    • x-s: 主要签名                               │
│    • x-t: 时间戳                                 │
│    • x-s-common: 公共参数                        │
│    • x-b3-traceid: 追踪ID                        │
└──────────────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────┐
│              4. 数据请求阶段                     │
├──────────────────────────────────────────────────┤
│  for 每个关键词:                                 │
│    for 每一页:                                   │
│      1. 构造API请求                              │
│      2. 携带签名参数                             │
│      3. 发送HTTP请求                             │
│      4. 解析JSON响应                             │
│      5. 提取帖子ID列表                           │
│                                                  │
│  for 每个帖子:                                   │
│      1. 获取帖子详情                             │
│      2. 获取评论列表                             │
│      3. 提取结构化数据                           │
└──────────────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────┐
│              5. 数据存储阶段                     │
├──────────────────────────────────────────────────┤
│  数据结构:                                       │
│  ┌────────────────────────────────────┐         │
│  │  ZhihuContent (帖子数据)            │         │
│  │  • content_id: 帖子ID               │         │
│  │  • title: 标题                      │         │
│  │  • content_text: 正文内容           │         │
│  │  • voteup_count: 点赞数             │         │
│  │  • user_nickname: 作者昵称          │         │
│  │  • content_url: 链接地址            │         │
│  └────────────────────────────────────┘         │
│                                                  │
│  ┌────────────────────────────────────┐         │
│  │  ZhihuComment (评论数据)            │         │
│  │  • content: 评论内容                │         │
│  │  • user_nickname: 评论者            │         │
│  │  • like_count: 点赞数               │         │
│  └────────────────────────────────────┘         │
│                                                  │
│  存储格式: JSON / CSV / DB                       │
│  存储位置: output/zhihu/                         │
└──────────────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────┐
│              6. 数据输出                         │
├──────────────────────────────────────────────────┤
│  📁 output/zhihu/search/                         │
│     └── zhihu_search_20260117.json               │
│  📁 output/zhihu/comments/                       │
│     └── zhihu_comments_20260117.json             │
└──────────────────────────────────────────────────┘
```

### 知乎平台特殊处理

```python
# 知乎数据采集关键步骤

1. 关键词搜索
   API: /api/v4/search_v3
   参数: q=关键词, type=content
   返回: 帖子列表

2. 获取帖子详情
   API: /api/v4/questions/{id}/answers
   或: /api/v4/articles/{id}
   返回: 完整内容、作者信息、点赞数等

3. 获取评论
   API: /api/v4/comment_v5/answers/{id}/root_comment
   参数: limit=每页数量
   返回: 一级评论列表

4. 签名生成
   方法: page.evaluate("window.mnsv2(...)")
   参数: uri + data + timestamp
   返回: 加密签名字符串
```

---

## 数据处理流程

### 数据流转图

```
原始数据 (JSON)
    │
    ▼
┌───────────────────┐
│   数据清洗        │
├───────────────────┤
│ • 去除HTML标签    │
│ • 提取关键信息    │
│ • 格式标准化      │
└───────────────────┘
    │
    ▼
┌───────────────────┐
│   数据筛选        │
├───────────────────┤
│ • 关键词匹配      │
│ • 热度阈值过滤    │
│ • 时间范围筛选    │
└───────────────────┘
    │
    ▼
┌───────────────────┐
│   数据分类        │
├───────────────────┤
│ • 按公司分类      │
│ • 按职位分类      │
│ • 按类型分类      │
└───────────────────┘
    │
    ▼
┌───────────────────┐
│   数据存储        │
├───────────────────┤
│ • 结构化存储      │
│ • 建立索引        │
│ • 数据持久化      │
└───────────────────┘
    │
    ▼
可用数据
```

### 数据筛选策略

#### 1. 关键词筛选

```python
# 一级关键词（标题匹配）
primary_keywords = [
    "招聘", "内推", "校招", "实习",
    "工程师", "开发", "产品经理"
]

# 二级关键词（内容匹配）
secondary_keywords = [
    "职位", "薪资", "要求",
    "投递", "简历", "面试"
]

# 三级关键词（公司名）
company_keywords = [
    "字节跳动", "腾讯", "阿里",
    "美团", "京东", "百度"
]
```

#### 2. 质量筛选

```python
# 热度阈值
if voteup_count >= 10:        # 点赞数>=10
    if comment_count >= 5:    # 评论数>=5
        return "高质量"

# 时效性
if (current_time - publish_time) <= 30天:
    return "近期有效"

# 权威性
if "官方" in author_name or "HR" in author_name:
    return "权威来源"
```

#### 3. 数据分类

```python
# 按信息类型分类
categories = {
    "校招实习": ["实习", "校招", "应届"],
    "社招内推": ["社招", "内推", "经验"],
    "高端岗位": ["专家", "架构师", "总监"],
    "技术岗位": ["工程师", "开发", "算法"],
    "产品岗位": ["产品", "运营", "设计"]
}
```

### 数据结构化示例

**原始数据:**
```json
{
  "title": "字节跳动2024春季校园招聘启动！",
  "content": "我们正在招募Java工程师、算法工程师...",
  "voteup_count": 156,
  "user_nickname": "字节跳动招聘"
}
```

**结构化后:**
```json
{
  "company": "字节跳动",
  "recruitment_type": "校招",
  "position_type": "技术",
  "positions": ["Java工程师", "算法工程师"],
  "quality_score": 8.5,
  "publish_date": "2024-03-15",
  "source": "知乎官方",
  "url": "https://zhihu.com/..."
}
```

---

## 使用流程

### 场景1: 一次性数据采集

```bash
# 1. 配置关键词
vim config/base_config.py
KEYWORDS = "字节跳动内推,腾讯招聘"

# 2. 运行爬虫
./run_crawler.sh

# 3. 查看结果
ls output/zhihu/search/
cat output/zhihu/search/zhihu_search_*.json | jq .
```

### 场景2: 定时监控招聘信息

```bash
# 1. 创建定时任务
crontab -e

# 2. 添加定时执行（每天早上9点）
0 9 * * * cd /path/to/MediaCrawler && ./run_crawler.sh

# 3. 配置增量存储
# 修改base_config.py，启用数据库存储
SAVE_DATA_OPTION = "sqlite"  # 自动排重
```

### 场景3: 多平台联合采集

```bash
# 1. 采集知乎招聘信息
python main.py --platform zhihu --lt cookie --type search

# 2. 采集小红书内推信息
python main.py --platform xhs --lt qrcode --type search

# 3. 采集B站校招视频
python main.py --platform bili --lt cookie --type search

# 4. 合并分析
python tools/merge_and_analyze.py
```

### 场景4: 数据分析和可视化

```python
# analysis.py
import json
import pandas as pd

# 1. 读取数据
with open('output/zhihu/search/zhihu_search_xxx.json') as f:
    data = json.load(f)

# 2. 转换为DataFrame
df = pd.DataFrame(data)

# 3. 数据分析
company_counts = df['title'].extract_company_names().value_counts()
position_types = df['content'].classify_positions()

# 4. 可视化
company_counts.plot(kind='bar')
```

---

## 后续优化方向

### 短期优化 (1-2周)

- [ ] **智能筛选规则**
  - 基于机器学习的职位分类
  - 薪资范围自动提取
  - 公司规模自动识别

- [ ] **数据质量提升**
  - 去重算法优化
  - 数据验证规则
  - 异常数据处理

- [ ] **用户体验优化**
  - WebUI界面优化
  - 实时进度显示
  - 错误提示优化

### 中期优化 (1-2月)

- [ ] **多平台整合**
  - 统一数据格式
  - 跨平台数据关联
  - 综合评分系统

- [ ] **智能推荐**
  - 基于用户画像推荐职位
  - 相似职位推荐
  - 薪资趋势预测

- [ ] **通知系统**
  - 实时推送新职位
  - 邮件/短信通知
  - 微信/钉钉机器人

### 长期优化 (3-6月)

- [ ] **分布式爬取**
  - 多机协同爬取
  - 任务队列系统
  - 负载均衡

- [ ] **数据分析平台**
  - 可视化仪表盘
  - 趋势分析图表
  - 导出报告功能

- [ ] **API服务**
  - RESTful API
  - 第三方集成
  - 移动端支持

---

## 附录

### A. 配置文件说明

| 参数 | 说明 | 推荐值 |
|------|------|--------|
| PLATFORM | 目标平台 | zhihu |
| KEYWORDS | 搜索关键词 | 逗号分隔 |
| CRAWLER_MAX_NOTES_COUNT | 爬取数量 | 50-100 |
| SAVE_DATA_OPTION | 存储格式 | json/db |
| ENABLE_IP_PROXY | 代理开关 | False |
| HEADLESS | 无头模式 | True(服务器) |

### B. 常见问题

**Q1: 如何避免被封？**
A: 控制请求频率、使用代理池、模拟真实用户行为

**Q2: Cookie多久失效？**
A: 通常7-30天，建议配置自动刷新机制

**Q3: 如何提高数据质量？**
A: 多关键词组合、设置热度阈值、二次筛选验证

### C. 性能指标

| 指标 | 数值 |
|------|------|
| 单次爬取时间 | 5-15分钟 |
| 数据准确率 | >90% |
| 去重率 | >95% |
| 并发支持 | 1-5个 |

---

**文档版本**: v1.0
**最后更新**: 2026-01-17
**维护者**: Claude Code Assistant
